ALGORITHM palindrome    // gag , php , kayak 
VAR
name:string; 
i,j:INTEGER;   
BEGIN
// if string is emptp or has one char
    IF (name="" || name(i).length ===1) THEN
        return : "wrong";
    else-if ( name(i)=== name.length-1)
    FOR i 0 min TO name.length-1 STEP 1  DO
        return palindrome;
        else
        return not palindrome;
    END_FOR
    END_IF
END